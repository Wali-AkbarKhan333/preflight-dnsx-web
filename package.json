{
  "name": "dnsx-web-preflight",
  "version": "2.6.2",
  "private": true,
  "description": "Private multi-user DNS/MX preflight UI using dnsx with canonical results, serial scanning, persistent checkpoints, pause/resume, partial exports, restart recovery, and resumable jobs.",
  "type": "module",
  "scripts": {
    "start": "node --disable-warning=ExperimentalWarning server.js",
    "dev": "node --disable-warning=ExperimentalWarning --watch server.js",
    "test": "node --disable-warning=ExperimentalWarning --test"
  },
  "engines": {
    "node": ">=22.5"
  }
}
